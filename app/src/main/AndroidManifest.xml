<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="real_deal.bsis">

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <application
        android:name=".config.AppSugerConfig"
        android:allowBackup="true"
        android:largeHeap="true"

        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".main_activity.AddDonationFromBatch"></activity>
        <activity android:name=".main_activity.Connection_port" />
        <activity
            android:name=".main_activity.EditContact"
            android:label="Edit Donor Contact" />
        <activity android:name=".main_activity.EditDonation" />
        <activity
            android:name=".main_activity.AddDonor"
            android:label="@string/add_donor" />
        <activity
            android:name=".main_activity.AddDonorDeferal"
            android:label="@string/manage_deferral" />
        <activity
            android:name=".main_activity.DonationDetail" />
        <activity
            android:name=".main_activity.BatchDetail" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main_activity.Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main_activity.FindDonorActivity"
            android:label="@string/find_donor" />
        <activity
            android:name=".main_activity.Barcode"
            android:label="@string/barcode" />
        <activity
            android:name=".main_activity.AddDonationActivity"
            android:label="@string/add_donation" />
        <activity android:name=".main_activity.Donor_Detail" />

        <meta-data
            android:name="DATABASE"
            android:value="bsis.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="false" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="real_deal.bsis.model" />
        <meta-data
            android:name="ENCRYPTION_KEY"
            android:value="BSIS@ETHIOPIA_SEQURING_SQLLITE_DB@Mobile_Config_AddisAbaba_123123123" />



    </application>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>